use petgraph::adj::NodeIndex;
use petgraph::Graph;

pub struct GameCell {}

impl GameCell {
    fn new(&mut self) {}
}

pub struct Board {
    head: NodeIndex,
}

impl Board {
    fn new(&mut self) {}
}
